import Link from 'next/link'
import Layout from '../components/layout'
import Head from '../components/head'

export default (props) => (
  <Layout>
    <p>User Uploads</p>
    <p>wait for the designer to do it.</p>
  </Layout>
)
